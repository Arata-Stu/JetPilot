[project]
name = "jetpilot-calibration-environment"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["numpy==1.26.4", "PyYAML==6.0.2", "rosbags==0.11.3"]

[tool.uv]
package = false
environments = ["sys_platform == 'linux'"]
