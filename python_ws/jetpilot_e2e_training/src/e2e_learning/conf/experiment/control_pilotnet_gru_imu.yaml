# @package _global_

defaults:
  - override /model: fusion
  - override /train: default

run:
  name: control_pilotnet_gru_imu

model:
  task: control
  backbone: pilotnet
  temporal: gru
  use_imu: true
  sequence_length: 4
  outputs: [steering, throttle]
