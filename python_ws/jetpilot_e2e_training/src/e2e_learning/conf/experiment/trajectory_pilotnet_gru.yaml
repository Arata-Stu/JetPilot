# @package _global_

defaults:
  - override /model: fusion
  - override /train: default

run:
  name: trajectory_pilotnet_gru

model:
  task: trajectory
  backbone: pilotnet
  temporal: gru
  use_imu: false
  sequence_length: 4
