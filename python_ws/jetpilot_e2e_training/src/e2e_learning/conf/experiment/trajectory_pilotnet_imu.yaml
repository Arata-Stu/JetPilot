# @package _global_

defaults:
  - override /model: fusion
  - override /train: default

run:
  name: trajectory_pilotnet_imu

model:
  task: trajectory
  backbone: pilotnet
  temporal: none
  use_imu: true
  sequence_length: 1
