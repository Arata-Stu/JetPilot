"""
event_camera_analyzer: ROSBag analyzer for SilkyEvCam / openeb_ros2 streams.
Diagnoses whether event_image flickering is caused by event stream drops (events/events_raw)
or image generator timer jitter/decode errors (event_image/diagnostics).
"""

__version__ = "0.1.0"
